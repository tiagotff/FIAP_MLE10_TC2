"""Pacote reservado para consolidação do loop de treino.

O loop de treino, métricas e early stopping já estão implementados e
funcionando em `recommender.pipeline.train` (desde a Etapa 3). Este
pacote fica reservado para uma eventual refatoração que extraia essa
lógica de dentro do script de pipeline para uma classe/módulo reutilizável
independente do DVC, caso isso venha a ser necessário na Etapa 4.
"""
